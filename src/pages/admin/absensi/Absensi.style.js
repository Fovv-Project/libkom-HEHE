import styled from "styled-components";

const AbsensiWrapper = styled.div`
    .wrapper {
        display: flex;
    }

    .content{
        width: 100%;
        padding-left: 5rem;
        padding-right: 5rem;
        padding-bottom: 1rem;
    }
`;

export default AbsensiWrapper;